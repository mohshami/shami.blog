{
  "name": "shami.blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hugo && pagefind --site public",
    "dev": "hugo && pagefind --site public && hugo server -p 1313 --bind 0.0.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
